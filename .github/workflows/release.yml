on: release
jobs:
  test:
    name: Tests
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/setup-go@v1
        with:
          go-version: 1.13
        id: go

      - uses: actions/checkout@v1

      - name: Run tests
        run:  |
          export GO111MODULE=on
          go test -v
